{
  "name": "finances",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc --build",
    "start": "npm run build && node ./dist/index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
