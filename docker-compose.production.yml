# Note about DNS:
#   using DOCKER_HOST results in the host getting the wrong DNS
#   configuration, so we use known good public nameservers
#   instead of the misconfigured ones

services:
  nginx:
    restart: always
    dns:
      - 8.8.8.8
      - 8.8.4.4
    volumes:
      - /share/homes/qnappy/home-automation/nginx.conf:/etc/nginx/nginx.conf:ro
      - /share/homes/qnappy/home-automation/letsencrypt:/etc/letsencrypt:ro

  certbot:
    restart: always
    dns:
      - 8.8.8.8
      - 8.8.4.4
    volumes:
      - /share/homes/qnappy/home-automation/letsencrypt:/etc/letsencrypt
      - /share/homes/qnappy/home-automation/certbot_sa_key.json:/root/.config/gcloud/application_default_credentials.json:ro
      
  homelink:
    restart: always
    dns:
      - 8.8.8.8
      - 8.8.4.4
    volumes:
      - /share/homes/qnappy/home-automation/homelink_sa_key.json:/root/.config/gcloud/application_default_credentials.json:ro
